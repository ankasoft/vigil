{
  "name": "vigil-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:migrate": "wrangler d1 execute vigil --file=migrations/0001_init.sql && wrangler d1 execute vigil --file=migrations/0002_notifications.sql && wrangler d1 execute vigil --file=migrations/0003_hosts.sql",
    "db:migrate:remote": "wrangler d1 execute vigil --remote --file=migrations/0001_init.sql && wrangler d1 execute vigil --remote --file=migrations/0002_notifications.sql && wrangler d1 execute vigil --remote --file=migrations/0003_hosts.sql"
  },
  "dependencies": {
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.91.0"
  }
}
